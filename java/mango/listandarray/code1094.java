package mango.listandarray;

/**
 * 车上最初有 capacity 个空座位，车只能向一个方向行驶（不能掉头或改变方向）
 *
 * 给定整数 capacity 和一个数组 trips
 * trips[i]=[passengerNumber_i, from_i, to_i]表示第 i 次旅行有 passengerNumber_i 名乘客，
 * 接他们和放他们的位置分别是 from_i 和 to_i，这些位置是从汽车的初始位置向东的公里数
 * 当且仅当你可以在所有指定的行程中接送所有乘客时，返回 true，否则请返回 false
 *
 * 输入：trips = [[2,1,5],[3,3,7]], capacity = 4
 * 输出：false
 *
 * 输入：trips = [[2,1,5],[3,3,7]], capacity = 5
 * 输出：true
 */
public class code1094 {
}
